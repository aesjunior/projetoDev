program ProjetoWK;

uses
  Vcl.Forms,
  uCadPedVenda in 'uCadPedVenda.pas' {Form2},
  uSelitens in 'uSelitens.pas' {Form1};

{$R *.res}

begin
  Application.Initialize;
  Application.MainFormOnTaskbar := True;
  Application.CreateForm(TForm2, Form2);
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
